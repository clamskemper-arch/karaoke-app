# Tests: In-Memory-DB + Wegwerf-Ordner, damit die echte Dev-DB / data/songs
# unangetastet bleiben.
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
karaoke.storage.base-dir=target/test-storage
karaoke.share.dir=target/test-share
